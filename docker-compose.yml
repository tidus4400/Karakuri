services:
  postgres:
    image: postgres:16
    container_name: karakuri-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: automation
      POSTGRES_PASSWORD: automation
      POSTGRES_DB: karakuri
    ports:
      - "5432:5432"
    volumes:
      - karakuri_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U automation -d karakuri"]
      interval: 10s
      timeout: 5s
      retries: 5

  orchestrator:
    container_name: karakuri-orchestrator
    build:
      context: .
      dockerfile: docker/Dockerfile.orchestrator
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: Host=postgres;Port=5432;Database=karakuri;Username=automation;Password=automation
      ConnectionStrings__PlatformConnection: Host=postgres;Port=5432;Database=karakuri;Username=automation;Password=automation
    ports:
      - "5010:8080"
    depends_on:
      postgres:
        condition: service_healthy

  web:
    container_name: karakuri-web
    build:
      context: .
      dockerfile: docker/Dockerfile.web
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      Orchestrator__BaseUrl: http://orchestrator:8080
    ports:
      - "5020:8080"
    depends_on:
      orchestrator:
        condition: service_started

  runner:
    container_name: karakuri-runner
    build:
      context: .
      dockerfile: docker/Dockerfile.runner
    restart: unless-stopped
    environment:
      DOTNET_ENVIRONMENT: Production
      Runner__ServerUrl: http://orchestrator:8080
      Runner__RegistrationToken: ""
      Runner__Name: docker-runner
      Runner__Tags: docker
      Runner__CredentialFile: /data/runner.credentials.json
    volumes:
      - karakuri_runnerdata:/data
    depends_on:
      orchestrator:
        condition: service_started

volumes:
  karakuri_pgdata:
  karakuri_runnerdata:
